apiVersion: apps/v1
kind: Deployment
metadata:
  name: flash-gateway
spec:
  replicas: 2 # Requisito: Alta Disponibilidade
  selector:
    matchLabels:
      app: flash-gateway
  template:
    metadata:
      labels:
        app: flash-gateway
    spec:
      containers:
      - name: gateway
        image: ederdcsp18/flash-gateway:latest # Imagem que subiremos pro Docker Hub
        ports:
        - containerPort: 8000
        resources: # Requisito: Gerenciamento de Recursos
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
        readinessProbe: # Requisito: Saúde da Aplicação
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: flash-gateway-service
spec:
  selector:
    app: flash-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer # Expõe o Gateway para a internet